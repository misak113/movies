{
	"name": "@misak113/movies",
	"version": "1.0.0",
	"description": "The movies",
	"main": "main.js",
	"scripts": {
		"start": "electron .",
		"install": "npm install --prefix=front && npm install --prefix=back",
		"prepare": "npm run build",
		"build": "npm run build-front",
		"build-front": "npm run build --prefix=front",
		"pack-win-x64": "electron-packager . movies --platform=win32 --arch=x64 --out=dist"
	},
	"repository": "https://github.com/misak113/movies",
	"keywords": [
		"Movies"
	],
	"author": "Michael Zabka",
	"license": "MIT",
	"devDependencies": {
		"electron": "^13.6.6",
		"electron-packager": "^13.0.1"
	}
}
